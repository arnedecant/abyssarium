{
  "name": "abyssarium-web",
  "version": "0.1.0",
  "type": "module",
  "homepage": "https://arnedecant.github.io/abyssarium",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:gh-pages": "BASE_PATH=/abyssarium/ pnpm run build",
    "preview": "vite preview",
    "deploy": "pnpm run build:gh-pages && gh-pages -d dist"
  },
  "dependencies": {
    "@tensorflow-models/pose-detection": "^2.1.3",
    "@tensorflow/tfjs": "^4.22.0",
    "@tensorflow/tfjs-backend-webgl": "^4.22.0",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "gh-pages": "^6.1.1",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
